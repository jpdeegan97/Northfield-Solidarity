# NS-SWB-006-VERSION — Versioning & Compatibility

Versioned: Request Envelope schema; model registry; routing policies; planner config; tool adapters.

Compatibility: parse N-1 envelope versions; requests can pin model/policy/plan versions.

Reproducibility: deterministic mode pins model versions, decoding settings, tool versions, cache behavior.

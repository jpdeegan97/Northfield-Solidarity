# Appendix A — Practical MVP Cut

MVP: ingress + envelope; classifier; GGE gate; 3-tier model registry; rules-based planner; executor with retries/fallbacks; LUM telemetry; deterministic mode.

MVP2: ensemble plans; automated schema validation; learning loop; caching for approved classifications; cross-region/provider redundancy.

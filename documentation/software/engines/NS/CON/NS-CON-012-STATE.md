# NS-CON-012-STATE — State Model

Connection:
CREATED → VALIDATED → ENABLED → RUNNING → (PAUSED | ERROR) → DISABLED

Run:
QUEUED → RUNNING → (SUCCEEDED | PARTIAL | FAILED)

DLQ Item:
ENQUEUED → (REPLAYED | DISMISSED | EXPIRED)

Schema:
DISCOVERED → VERSIONED → (DRIFT_DETECTED → MIGRATED → CONFIRMED)

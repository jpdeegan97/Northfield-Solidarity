FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt /app/requirements.txt
RUN pip install --no-cache-dir -r /app/requirements.txt

COPY db /app/db

# Requires POSTGRES_DSN in env_file (postgresql+asyncpg://...)
CMD ["bash", "-lc", "alembic -c db/alembic.ini upgrade head"]